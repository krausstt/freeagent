# Credentials and live league data must never be committed.
# .ffdraft.json holds ESPN session cookies, equivalent to your password.
.ffdraft.json
cockpit_data.json
report_snapshot.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Android build output and the generated WebView asset (regenerated by
# android/build_assets.py from dashboard/index.html on every build).
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/assets/index.html
*.jks
*.keystore
